# Engineering Retrospective — 2026-05-W20

> Week of 2026-05-12 → 2026-05-19  
> Generated: 2026-05-19

---

## Highlights

- **Memory v2 shipped** — hybrid retrieval, prompt caching, budget caps, smart routing, dedup/decay/summaries/contradictions, ConvoMem. Largest single feature this week (+6,874 lines diff).
- **HealthKit integration merged** — Shreyash's deep mobile integration including tier-1 onboarding, home widgets, and care screens landed cleanly into main.
- **Caregiver platform built out** — care-group code flow (DB migration → endpoints → mobile UI → auth gate), caregiver dashboard, self-care wizard, and mood/support fields all shipped.
- **v0.5.1.0 milestone** — all dev branches merged and tagged; canary monitor with auto-promote + GH Actions alerts now gating production.
- **Design system enforced** — Trust Indigo tokens applied across 4 flagged surfaces (F-001 through F-006); off-system blues eliminated from auth and invite flows.
- **25 new test files added** — strong test coverage push for memory subsystem and mobile service normalizers.

---

## By Author

### Aryan Motgi (combined: local + aryanmotgi GitHub identity)
| Metric | Value |
|---|---|
| Total commits | 48 |
| Lines added | ~27,596 |
| Lines removed | ~7,582 |
| Primary areas | `apps/web/src/lib/memory/`, `apps/mobile/app/`, `apps/mobile/src/services/`, `apps/web/src/components/`, `apps/web/src/components/onboarding/`, `.github/workflows/` |

**Key contributions:** Memory v2 full stack (retrieval, cache, budget, routing, ConvoMem), canary/CI monitor pipeline, caregiver dashboard, web onboarding port, mobile auth polish, iOS Live Activities + HealthKit auto-fetch, design token enforcement, care-group invite revoke, App Store compliance fixes.

### Shreyash Somani
| Metric | Value |
|---|---|
| Total commits | 13 |
| Lines added | ~9,002 |
| Lines removed | ~906 |
| Primary areas | `apps/mobile/app/`, `apps/mobile/src/components/home/`, `apps/web/src/app/api/care-group/`, `apps/web/src/lib/db/migrations/` |

**Key contributions:** HealthKit integration (tier-1 onboarding, home widgets, care screens), care-group code endpoints + migration 011, caregiver lane in AuthGate, patient/caregiver onboarding split, feature flags + deprecation headers, relationship-aware AI greeting, DEV mock clinical records for simulator, E2E care-group code tests.

### Rahil
| Metric | Value |
|---|---|
| Total commits | 0 |
| Lines added | 0 |
| Lines removed | 0 |

No commits observed this week.

---

## By Category

| Type | Count |
|---|---|
| `feat` | 19 |
| `fix` | 18 |
| `chore` | 5 |
| `style` | 4 |
| `test` | 2 |
| `ci` | 1 |
| `docs` | 0 |
| `security` | 0 |
| `perf` | 0 |

Notable: high fix:feat ratio (18:19) this week — reflects active iteration on new features as they landed. Zero `docs:` commits and zero formally tagged `security:` commits.

---

## Biggest Changes

Ranked by total diff size (insertions + deletions):

| Rank | Hash | Author | Description | Lines |
|---|---|---|---|---|
| 1 | `7128098` | Aryan | `feat(memory): v2 — hybrid retrieval, prompt cache, budget caps, smart routing (#48)` | 6,874 |
| 2 | `2e51cc1` | Shreyash | `feat(mobile): healthkit integration + tier-1 onboarding + home widgets + care screens` | 6,573 |
| 3 | `e87b6c6` | Aryan | `feat(mobile): auth flow polish + add med/appt + iOS Live Activities/scanner/critical alerts/bg fetch (#38)` | 5,049 |
| 4 | `a05f533` | Aryan | `chore: merge all dev branches → main (v0.5.1.0)` | 4,384 |
| 5 | `698d858` | Aryan | `feat(web): port mobile onboarding flow + visual parity (#43)` | 3,521 |

---

## Trends

### Test Coverage
- **25 new test files added** this week — a major push.
- New coverage areas: `memory-conflict`, `decay`, `convomem`, `summary-retrieval`, `budget`, `embed`, `extract`, `gate`, `memory.snapshot`, `rerank`, `retrieve`, `validators`, `normalizers` (mobile), `pure` (mobile).
- Modified: `healthkit/replace`, `system-prompt`, `production-monitor` E2E.
- Mobile test coverage improved with `normalizers.test.ts` and `pure.test.ts` in `src/services/internal/__tests__/`.

### Documentation
- **0 `docs:` commits** this week. Memory v2 is a large new subsystem with no accompanying developer docs yet.

### Security / Compliance
- **0 formally tagged `security:` commits**, but `ee863c5` (`fix: App Store compliance — privacy manifest, entitlements, disclaimer modal`) is security-adjacent and important.
- No credential-adjacent changes flagged.

### CI / Canary
- Canary monitor took **3 commits to stabilize** (`#49` feat → `#50` fix wrong DB → `#51` fix exit code). Now stable with auto-promote + GH Actions issue alerts.

---

## Risks / Watch List

1. **Canary churn** — The canary monitor needed 3 rapid iterations (#49 → #50 → #51) before stabilizing. Worth verifying no false auto-promotes fired in production during that window.

2. **No Rahil activity** — Zero commits from Rahil this week. FHIR layer and onboarding flows (his ownership area) had no changes. Worth checking in — any blockers?

3. **Docs gap for Memory v2** — The memory subsystem (`apps/web/src/lib/memory/`) is now a core architectural layer with hybrid retrieval, caching, budget caps, and ConvoMem. No developer documentation committed alongside it. Risk: onboarding friction for future contributors.

4. **Multi-worktree merge commits on main** — `aryan/worktree1..4` branches were merged via `git merge` (not squash) into `aryan/dev`, which then squashed into main. Clean on main, but the worktree pattern should be documented so other devs don't create a similar pattern with less clean resolution.

5. **Large PRs without dedicated review** — `feat(memory): v2` (#48, 6,874 lines) and `feat(mobile): healthkit` (6,573 lines) are both very large single commits. Large diffs are harder to review and more likely to hide subtle bugs.

6. **Mobile test coverage still light outside services** — New tests are primarily in `src/services/internal/`. The HealthKit integration, home widgets, and onboarding screens themselves have no accompanying component-level tests.

---

## Action Items for Next Week

| # | Item | Owner | Priority |
|---|---|---|---|
| 1 | **Check in with Rahil** — confirm no blockers on FHIR/onboarding work; sync on what's planned for W21 | Aryan | High |
| 2 | **Write Memory v2 developer docs** — architecture overview, retrieval flow diagram, budget cap behavior, ConvoMem contract | Aryan | High |
| 3 | **Verify canary auto-promote accuracy** — review canary run history since #49 landed; confirm no false promote fired | Aryan | Medium |
| 4 | **Mobile component test coverage** — add basic render/interaction tests for HealthKit home widgets and caregiver onboarding screens | Shreyash | Medium |
| 5 | **Enforce `docs:` commit convention for new subsystems** — any PR adding a new `src/lib/` module should ship with at least a top-level README or JSDoc | All | Medium |
| 6 | **Medication observation data layer** (`3d5f7f2`) — confirm reconciliation E2E path is tested end-to-end; was landed without test coverage visible in log | Aryan | Medium |
| 7 | **App Store submission checklist** — privacy manifest and entitlements fixed; verify full submission flow in TestFlight before W21 | Aryan | Medium |
