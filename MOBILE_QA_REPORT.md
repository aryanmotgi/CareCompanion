# Mobile iOS QA Sweep — 2026-05-22

**Branch:** `aryan/dev`  
**Scope:** All 43 routes under `apps/mobile/app/`  
**Auditor:** aryan/dev automated QA sweep  
**Bugs found:** 35 (2 × P0, 8 × P1, 25 × P2)  
**Patches:** see `PATCHES/ios-qa-sweep.md`

---

## Screen Index

All routes read during this sweep:

`(tabs)/_layout` · `(tabs)/care` · `(tabs)/chat` · `(tabs)/community` · `(tabs)/index` · `(tabs)/labs` · `(tabs)/scan` · `(tabs)/settings` · `(tabs)/trials` · `_layout` · `appointments` · `appointments/new` · `care-group-join` · `care-group-settings` · `care-relationship` · `care-type` · `caregiver-burnout` · `community/[id]` · `edit-care-group` · `emergency` · `health-connect` · `health-consent` · `health-records` · `health-replace-prompt` · `health-summary` · `insurance` · `journal` · `login` · `medications/new` · `notification-settings` · `notifications` · `onboarding-records` · `scanned-docs` · `search` · `setup` · `share-invite` · `signup` · `symptom-radar` · `timeline` · `treatment-cycle` · `visit-prep` · `voice-checkin` · `welcome`

---

## Bug Table

| # | Screen Route | Files | Bug | Severity | Repro | Suggested Fix |
|---|---|---|---|---|---|---|
| 1 | `/treatment-cycle` | `app/treatment-cycle.tsx:83` | Wrong SecureStore key `'authToken'` used to read session token; app stores all sessions under `'cc-session-token'` (see `src/services/auth.ts:33`). Token is always `null` → every fetch sends no `Authorization` header → API returns 401 → medications never load | **P0** | Open Treatment Cycle tab on any account → perpetual "Could not load treatment data" error | Change key to `'cc-session-token'` |
| 2 | `/notifications` | `app/notifications.tsx:80-82` | When the API responds with a non-2xx status, code calls `setLoading(false)` and returns early **without** calling `setError(true)`. User sees a permanently blank, non-loading screen with no error message or retry affordance. | **P0** | Disable network or use a test account that returns 404 → blank white screen, no error shown | Add `setError(true)` before the early `return` on line 81 |
| 3 | `/treatment-cycle` | `app/treatment-cycle.tsx:64-66` | Phase label logic is inverted. `dayInCycle <= cycleLengthDays - 5` (e.g. days 4–16 in a 21-day cycle) is labelled **"recovery"** when it should be **"rest"** (worst side-effects window). Days 17–21 are labelled **"rest"** but should be **"recovery"** (patient rebounds before next infusion). | **P1** | Add medication with notes `"cycle 1 of 6, day 8, every 21 days"` → screen shows "Recovery" badge | Change condition to `dayInCycle > cycleLengthDays - 5` → recovery |
| 4 | `/emergency` | `app/emergency.tsx:108` | `Linking.openURL('tel:911')` inside Alert `onPress` handler is neither `await`-ed nor `.catch()`-ed. On iOS Simulator and devices without telephony capability the promise rejects silently; no error is shown and no fallback fires. | **P1** | Run in Simulator → tap "Call 911" → nothing happens, no error, no feedback | `void Linking.openURL('tel:911').catch(() => Alert.alert('Unable to place call'))` |
| 5 | `/emergency` | `app/emergency.tsx:115` | Same as #4: `Linking.openURL('tel:988')` in `handleCall988()` is a bare unhandled call. | **P1** | Same device/simulator path → tap 988 button → silent failure | Add `.catch(() => Alert.alert('Unable to place call'))` |
| 6 | `/emergency` | `app/emergency.tsx:270` | `emergencyContactPhone` from the user's profile is passed directly into a `tel:` URI. Phone numbers stored with spaces, parentheses, or dashes (e.g. `(555) 123-4567`) break `tel:` on some iOS versions and devices. | **P1** | Set emergency contact phone to `(555) 123-4567` → tap Call → call may silently fail | Strip non-digits: `tel:${emergencyContactPhone.replace(/\D/g, '')}` |
| 7 | `/health-connect` | `app/health-connect.tsx:505` | `console.log('[Calendar] sync result:', result)` ships to production. The `result` object from `syncMedicalCalendarEvents` includes matched medical calendar event titles and doctor/location data — PHI. | **P1** | Sync calendar in production build → check Xcode device log → event titles visible | Remove the `console.log` line entirely |
| 8 | `/setup` | `app/setup.tsx:441` | `console.error('[Setup] Error:', e)` — the error object `e` may include the profile patch payload (cancer type, medications, date of birth) as part of the serialised error message. Ships in production builds. | **P1** | Trigger a profile patch error → check production Sentry / device log | `console.error('[Setup] patch error')` — drop `e` argument |
| 9 | `/(tabs)/index` | `app/(tabs)/index.tsx:387` | `console.error(err)` on home screen HealthKit sync failure. The raw `err` from `syncHealthKitData` can include API response bodies containing auth tokens or care-profile data. | **P1** | Fail HealthKit sync on home screen → check device log | `console.error('[Home] healthkit sync failed')` — drop `err` argument |
| 10 | `/health-connect` | `app/health-connect.tsx:437` | Hard-timeout handler fires `console.warn('[HealthKit] connect hard-timeout — forcing success')` in production, then transitions to "Connected" state even when HealthKit permissions were explicitly **denied** by the user. User believes they connected HealthKit when they haven't. | **P1** | Deny HealthKit permission prompt → wait 10s → screen shows "Connected" | Remove `console.warn`; only force-success when `permResult?.granted !== false` |
| 11 | `/caregiver-burnout` | `app/caregiver-burnout.tsx:136,149` | `key={i}` (array index) used for `assessment.factors` and `assessment.suggestions` list items. If the AI returns a different number of factors on the next poll the wrong items animate or update. | P2 | Trigger two consecutive AI assessments with different factor counts → wrong items animate | Use `key={factor}` / `key={s}` (item content as key) |
| 12 | `/(tabs)/chat` | `app/(tabs)/chat.tsx:263` | `key={i}` for `AnimatedDot` components in typing indicator. When the dot count changes React reuses the wrong animated node, breaking the pulsing animation. | P2 | Observe typing indicator during multi-turn AI stream | `key={`dot-${i}`}` |
| 13 | `/(tabs)/chat` | `app/(tabs)/chat.tsx:185` | `key={i}` for conversation rows in the list. Deleting a conversation causes all rows below it to re-render with wrong identity/animation. | P2 | Delete a conversation mid-list → remaining rows flash | `key={conv.id}` |
| 14 | `/visit-prep` | `app/visit-prep.tsx:66,80,87,94,103,116,125,141,145` | `key={i}` on every branch of the inline markdown renderer (skeleton, h1, h2, h3, check row, bullet, table, spacer, paragraph). Any update to the prep document causes the entire rendered subtree to remount. | P2 | AI generates a multi-section visit prep doc; tap "Regenerate" → full flicker/remount | Composite key: `key={`${nodeType}-${i}`}` |
| 15 | `/welcome` | `app/welcome.tsx:180` | `key={i}` for welcome-screen pager dot indicators. Static list here so no crash, but best practice violation; dots won't animate correctly if page order changes. | P2 | Swipe through welcome pages → dot indicators may not transition smoothly | `key={`dot-${i}`}` |
| 16 | `/health-summary` | `app/health-summary.tsx:35,51,63,69` | `key={i}` across all four branches of the markdown summary renderer. | P2 | Load health summary on a fresh profile then refresh → possible element reuse | `key={`summary-${nodeType}-${i}`}` |
| 17 | `/health-connect` | `app/health-connect.tsx:238,308,650` | `key={i}` in institution list, permission toggle list, and step dot list. | P2 | Rapidly animate through HealthKit setup steps → step dots flicker | Stable string keys per item |
| 18 | `/(tabs)/trials` | `app/(tabs)/trials.tsx:57,134` | `key={i}` for shimmer placeholders and trial result rows. Identity is lost when loading → loaded transition plays, causing animated cards to snap to wrong positions. | P2 | Reload trials list after initial load → row-swap animation incorrect | `key={trial.nctId ?? trial.id}` for results |
| 19 | `/(tabs)/labs` | `app/(tabs)/labs.tsx:291` | `key={i}` for skeleton `<Card />` placeholders. Minor for static skeletons but inconsistent. | P2 | Reload labs tab → skeleton shimmer may stutter during transition | `key={`lab-skeleton-${i}`}` |
| 20 | `/(tabs)/community` | `app/(tabs)/community.tsx:73` | `key={i}` for `ShimmerSkeleton` components during loading state. | P2 | Load community tab on slow connection → shimmer animation stutters | `key={`community-skeleton-${i}`}` |
| 21 | `/voice-checkin` | `app/voice-checkin.tsx` | The screen immediately calls `setState('done')` with hardcoded stub data `{ pain: 4, mood: 'okay', energy: 'moderate', notes: 'Voice stub placeholder' }` on mount. The recording, processing, and transcribing UI states are never displayed. Users see instant fake results. | P2 | Open Voice Check-in → jumps straight to results with placeholder text | Gate stub behind `if (__DEV__)` with `setTimeout`; add real state machine |
| 22 | `/appointments/new` | `app/appointments/new.tsx:18` | Imports the module-level singleton `apiClient` directly from `../../src/services/api` instead of calling `useProfile().apiClient`. Every other screen uses the profile-context client. Inconsistent; if per-user auth scope or interceptors are added to the context client, `appointments/new` will be silently left behind. | P2 | Create appointment via `/appointments/new` → uses global singleton, not per-session auth client | `const { apiClient } = useProfile()` |
| 23 | `/(tabs)/_layout` | `app/(tabs)/_layout.tsx:273` | `screenOptions` spread uses the pattern `...(({ contentStyle: {...}, sceneContainerStyle: {...} }) as any)` — the inner expression is an object literal wrapped in an IIFE-like grouping and cast to `any`. The result is that `contentStyle` and `sceneContainerStyle` are stripped from `screenOptions`; tab content background flashes non-theme colour on navigation. | P2 | Navigate between tabs in dark mode → brief white flash on tab content area | Inline the props: `screenOptions={{ headerShown: false, contentStyle: { backgroundColor: theme.bg } }}` |
| 24 | `/medications/new` | `app/medications/new.tsx:101` | `as any` cast on the `apiClient.medications.create(payload, csrfToken)` call silences TypeScript. The payload uses camelCase keys (`doctorName`, `careProfileId`) which may not match the backend's expected snake_case schema, resulting in silent 422s. | P2 | Create a medication → check network tab for 422 from backend | Remove `as any`; define and use a typed `CreateMedicationPayload` interface |
| 25 | `/signup` | `app/signup.tsx:284-286` | Hardcoded `setTimeout(..., 950)` before `router.replace('/care-type')`. On slow/older devices the 950ms can expire while the splash animation is still rendering, causing a visual jump. The timing is empirical and will regress on hardware changes. | P2 | Use iPhone SE (1st gen) in debug build → navigation fires before success overlay fully animates | Use `InteractionManager.runAfterInteractions` instead of fixed timeout |
| 26 | `/health-replace-prompt` | `app/health-replace-prompt.tsx:51,54` | `router.replace('/setup' as any)` — unchecked string route with `as any`. If `/setup` is renamed, the broken navigation is invisible to TypeScript. | P2 | Rename `setup.tsx` without updating this file → silent broken navigation | Use typed route constant |
| 27 | `/setup` | `app/setup.tsx:474` | `console.error('[Setup] onboarding/complete failed:', e)` ships to production. The error from the `/api/onboarding/complete` endpoint can include a response body with the full profile (name, cancer type, medications). | P2 | Fail onboarding complete → check device log | `console.error('[Setup] onboarding/complete failed')` — drop `e` |
| 28 | `/notification-settings` | `app/notification-settings.tsx` | The push-notifications enable Switch uses an `async onValueChange` handler. The Switch updates its visual state synchronously before the async `requestPermissions()` resolves. If the user denies the permission dialog the Switch stays `ON` until the component re-mounts. | P2 | Toggle push-notifications ON → deny system prompt → switch stays ON | Track `pending` state; reset switch toggle if permission is denied |
| 29 | `/share-invite` | `app/share-invite.tsx:95-104` | The async IIFE in `useEffect` calls `requestPermissions()` but does not `.catch()` the result. If the notification module is unavailable the IIFE throws an unhandled rejection logged to the JS runtime. | P2 | Use a device where `expo-notifications` native module fails → JS unhandled rejection in console | Add `.catch(() => {})` after `requestPermissions()` call |
| 30 | `/login` | `app/login.tsx` | Apple Sign-In and Google Sign-In buttons are not disabled while `socialLoading` is `true`. Double-tapping either button launches two concurrent OAuth sessions; whichever resolves second may overwrite the first token. | P2 | Double-tap "Sign in with Apple" rapidly → two concurrent auth sessions | Set `disabled={!!socialLoading}` on both social buttons |
| 31 | `/(tabs)/scan` | `app/(tabs)/scan.tsx` | `Animated` is imported from `react-native` but is never referenced in the component. Dead import increases bundle size. | P2 | Run `npm run deadcode` → flagged | Remove `Animated` from the import |
| 32 | `/health-summary` | `app/health-summary.tsx` | Cookie name selection uses `baseUrl.startsWith('https://')`. If `EXPO_PUBLIC_API_BASE_URL` is set with a trailing slash (e.g. `https://carecompanionai.org/`) the check still passes, but some deployments normalise to `https://carecompanionai.org` — the inconsistency could produce wrong cookie names if the env var is mis-configured. | P2 | Set `EXPO_PUBLIC_API_BASE_URL=https://host/` → wrong cookie header for session | Use `new URL(baseUrl).protocol === 'https:'` |
| 33 | `/journal` | `app/journal.tsx:446` | Journal entries are keyed by `entry.date` (e.g. `"2026-05-22"`). If a user submits multiple entries in one day (offline-queue replay, duplicate submission) and the response returns two entries with the same date, React will warn about duplicate keys and render only one row. | P2 | Submit journal entry while offline; go online; submit again → duplicate rows collapse | Key by `entry.id` if present, fall back to `entry.date` |
| 34 | `/care-group-settings` | `app/care-group-settings.tsx` | Comment states "List current members (placeholder; full member-row UI in follow-up)". The members section never fetches or displays actual group members — it is permanently empty. | P2 | Open Care Group Settings → Members section blank | Implement member list fetch + UI |
| 35 | `/(tabs)/labs` | `app/(tabs)/labs.tsx:212` | `router.push({ pathname: '/health-connect', ... } as any)` — `as any` cast on router push. Part of a wider pattern of 10+ `as any` navigation casts across the app that hide broken route strings from TypeScript. | P2 | Rename `/health-connect` → TypeScript does not catch the broken ref | Define an `AppRoutes` const enum; remove navigation `as any` casts |

---

## Summary by Severity

| Severity | Count | Description |
|---|---|---|
| **P0** | 2 | App-breaking: blank screens or permanently broken features |
| **P1** | 8 | Major: safety-critical (emergency calling), silent data loss, PHI in logs |
| **P2** | 25 | Moderate: UX regressions, code quality, minor data integrity |
| **Total** | **35** | Across 43 screens |

## Priority Fix Order

1. **Bug #1** — `treatment-cycle.tsx` wrong token key (P0) — medications never load
2. **Bug #2** — `notifications.tsx` silent error (P0) — blank screen
3. **Bugs #7,8,9,10,27** — PHI console leaks (P1) — ship compliance
4. **Bugs #4,5,6** — Emergency call failures (P1) — patient safety
5. **Bug #3** — Phase label wrong (P1) — incorrect medical information shown
6. **Bug #23** — Tab layout `as any` spread strips background styles (P2) — visual regression
7. **Bugs #11–20** — `key={i}` list index keys (P2) — animation bugs
